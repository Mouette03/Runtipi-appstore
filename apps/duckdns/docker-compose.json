{
  "schemaVersion": 2,
  "$schema": "https://schemas.runtipi.io/dynamic-compose.json",
  "services": [
    {
      "name": "duckdns",
      "image": "ghcr.io/linuxserver/duckdns:version-36a2e3a3",
      "volumes": [
        {
          "hostPath": "${APP_DATA_DIR}/data/config",
          "containerPath": "/config"
        }
      ],
      "environment": [
        {
          "key": "PUID",
          "value": "1000"
        },
        {
          "key": "PGID",
          "value": "1000"
        },
        {
          "key": "TZ",
          "value": "${TZ}"
        },
        {
          "key": "SUBDOMAINS",
          "value": "${DUCKDNS_SUBDOMAINS}"
        },
        {
          "key": "TOKEN",
          "value": "${DUCKDNS_TOKEN}"
        },
        {
          "key": "UPDATE_IP",
          "value": "${DUCKDNS_UPDATE_IP}"
        },
        {
          "key": "LOG_FILE",
          "value": "${DUCKDNS_ENABLE_LOG_FILE}"
        }
      ]
    }
  ]
}

